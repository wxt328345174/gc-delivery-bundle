# 安装说明

## 一、交付内容

交付包默认包含：

- `install.sh`
- `customer.conf`
- `packages/`
- `.internal/`

客户通常只需要执行 `install.sh`，不需要进入 `.internal/`。

## 二、安装前准备

请在目标设备上确认：

- 操作系统为 `openEuler aarch64`
- 使用 `root` 用户执行安装
- 系统具备 `dnf`、`rpm`、`systemctl`
- 交付包已经完整复制到目标设备本地目录

## 三、如何修改配置

编辑同目录下的 `customer.conf`。

常见示例：

只做基础环境准备：

```bash
SOFTWARES=()
```

只安装 runtime：

```bash
SOFTWARES=("runtime")
```

安装 chromium 和向日葵：

```bash
SOFTWARES=("chromium" "sunlogin")
```

安装 Qt Creator：

```bash
SOFTWARES=("qt")
```

说明：

- `REBOOT_POLICY="prompt"`：安装结束后提示是否重启
- `REBOOT_POLICY="always"`：安装结束后自动重启
- `REBOOT_POLICY="never"`：安装结束后不自动重启

## 四、runtime 预编译 IGH 说明

如果交付包中同时包含如下文件：

```text
packages/ethcat_aarch64_<目标机uname-r>.tar.gz
```

则安装 `runtime` 时会优先直接安装该预编译 IGH 包，跳过现场编译，整体安装速度会明显更快。

如果没有该文件，脚本会自动回退到原有源码编译流程，功能上仍然可以安装，只是耗时更长。

## 五、如何执行

进入交付目录后执行：

```bash
bash install.sh
```

如果只想先检查流程和安装包，不真正安装：

```bash
bash install.sh --dry-run
```

如果配置文件不在默认位置，可以指定：

```bash
bash install.sh --config ./customer.conf
```

## 六、安装后检查

### 1. runtime

安装完成并重启后，可以检查：

```bash
ps aux | grep runtime
systemctl status ethercat
```

### 2. chromium

默认口径：

```text
用户名: gcuser
密码: gcuser
```

重启或重新登录图形界面后：

- 使用 `gcuser` 登录
- 从桌面菜单打开 Chromium

### 3. 向日葵

安装完成后，可检查：

```bash
rpm -qa | grep -i sunlogin
```

### 4. Qt Creator

安装完成后，可检查：

```bash
qtcreator --help
```

## 七、常见问题

### 1. 提示必须使用 root

请切换到 `root` 后重新执行：

```bash
sudo -i
bash install.sh
```

### 2. 提示缺少安装包

请确认 `packages/` 目录中的文件没有被删除或改名。

### 3. runtime 安装很慢

如果现场仍在编译 IGH，通常是因为交付包中没有放入匹配当前内核版本的：

```text
ethcat_aarch64_<uname-r>.tar.gz
```

### 4. 安装完成后未重启

某些软件需要重启后才能完全生效，特别是 `runtime`、图形自动登录和 Qt 桌面菜单相关配置。
